server:
  port: 8080

spring:
  application:
    name: racla-spring-proxy-service
  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
