server:
  port: 8080

spring:
  application:
    name: racla-spring-proxy-service-v2
  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
